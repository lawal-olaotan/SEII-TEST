import request from 'supertest';
import { subDivisionController } from './controller/index.js';


describe('HTTP Server', () => {

    //Test if the server is created successfully
    // Test if the correct CORS headers are set for all responses
    // Test if the URL is correctly parsed and passed to subDivisionController
    // Test successful response (200 status code)
    // Test error response (500 status code)
    // Test if the Content-Type header is set correctly for all responses

})
